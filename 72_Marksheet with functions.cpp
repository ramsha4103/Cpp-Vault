//This program was created to display a marksheet.
#include<iostream>
#include<string>
using namespace std;
int sum(int e, int u, int m, int c, int p)
float per(int) 
float grade(char)
int main()
{
	int e, u, m, c, p;
	char grade;
	float y,k;
	cout<<"Enter Marks in English out of 100 \n";
	cin>>e;
	cout<<"Enter Marks in Urdu out of 100 \n";
	cin>>u;
	cout<<"Enter Marks in Math out of 100 \n";
	cin>>m;
	cout<<"Enter Marks in Computer out of 100 \n";
	cin>>c;
	cout<<"Enter Marks in Physics out of 100 \n";
	cin>>p;
	y=sum(e,u,m,c,p);
	k=per(t);
	grade=grade(percentage)
	cout<<"Sum is "<<y;
	cout<<"Percentage is "<<percentage;
	cout<<"Grade is "<<grade;
	
}
int sum(int a,int b,int c,int d,int e)
{
	int t;
	t=a+b+c+d+e;
	return t;
}
float per(int u)
{
	float k;
	k=u/5;
	return k;
}
float grade(float d);
{
char c;
if(d>=80)
c='A';
else if(d>=60)
c='B';
else if(d>=40)
c='C';
else if(d<=39)
c='F';
return c
}
